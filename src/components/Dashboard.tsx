import React from 'react';

const Dashboard = () => {
    return (
        <div>
            <h1>Bem-vindo ao Dashboard!</h1>
            <p>Esta é uma área protegida que só pode ser acessada após o login.</p>
        </div>
    );
};

export default Dashboard;
